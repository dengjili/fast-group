<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!--使用注解-->
    <mvc:annotation-driven/>
    
    <!-- 自动扫描controller文件 -->
    <context:component-scan base-package="priv.dengjl.*.controller"/>

    <!--视图解析器-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <!--指定视图的前缀和后缀，Controller返回的String类型与这里的前后缀拼接，构成返回的视图页面地址-->
        <property name="prefix" value="/WEB-INF/html/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!--将链接中/p的静态的访问路径映射为URL，常用于加载html、js、css、图片、视频等静态资源-->
    <mvc:resources mapping="/html/js/**" location="/source/js/"/>
    <mvc:resources mapping="/html/img/**" location="/source/img/"/>
    <mvc:resources mapping="/html/css/**" location="/source/css/"/>
    
</beans>